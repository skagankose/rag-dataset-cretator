# =================================================
# RAG Dataset Creator Environment Configuration
# =================================================
# Copy this file to .env and fill in your values:
# cp env.template .env

# =================================================
# LLM Provider Configuration
# =================================================
# Choose your LLM provider: "openai" or "gemini"
LLM_PROVIDER=openai

# =================================================
# OpenAI Configuration
# =================================================
# Your OpenAI API key - Get one from https://platform.openai.com/api-keys
# Required if LLM_PROVIDER=openai
OPENAI_API_KEY=your_open_ai_api_key

# OpenAI model to use for question generation
# Options: gpt-4o-mini, gpt-4o, gpt-3.5-turbo, etc.
OPENAI_CHAT_MODEL=gpt-4.1

# Request timeout in seconds
OPENAI_TIMEOUT=60

# Maximum number of retries for failed requests
OPENAI_MAX_RETRIES=5

# =================================================
# Google Gemini Configuration
# =================================================
# Your Google AI API key - Get one from https://aistudio.google.com/app/apikey
# Required if LLM_PROVIDER=gemini
# IMPORTANT: You must also enable the Generative Language API in Google Cloud Console
# Visit: https://console.cloud.google.com/apis/api/generativelanguage.googleapis.com
GEMINI_API_KEY=your_gemini_api_key_here

# Gemini model to use for question generation
# Options: gemini-1.5-flash, gemini-1.5-pro, gemini-pro, etc.
GEMINI_CHAT_MODEL=gemini-1.5-flash

# Request timeout in seconds
GEMINI_TIMEOUT=60

# Maximum number of retries for failed requests
GEMINI_MAX_RETRIES=5

# =================================================
# Ingestion Pipeline Defaults
# =================================================
# Default chunk size in characters (100-5000)
DEFAULT_CHUNK_SIZE=1200

# Default overlap between chunks in characters (0-1000)
DEFAULT_CHUNK_OVERLAP=200

# Default number of questions to generate per chunk (1-5)
DEFAULT_TOTAL_QUESTIONS=10

# Whether to strip standard Wikipedia sections (References, External links, etc.)
# Set to 'true' or 'false'
STRIP_SECTIONS=true

# =================================================
# Application Configuration (Optional)
# =================================================
# Application environment (dev, prod)
# APP_ENV=dev

# Backend port (if different from 8051)
# BACKEND_PORT=8051

# Data directory for storing processed articles
# DATA_DIR=/app/data

# =================================================
# Frontend Configuration (Optional)
# =================================================
# Backend API URL for frontend to connect to
# VITE_API_URL=http://localhost:8051 
